# 0329-04 DWS 测试日志

时间：2026-03-29

说明：
- 本文件记录本次对 DWS 可用服务的真实账号测试。
- 每条记录包含请求、返回、错误信息。
- 对于高风险或不可逆操作，默认不执行删除/撤回/移除类动作，除非另有明确确认。

## 追加记录

时间：2026-03-29 04:10:27 CST

操作：
- 请求：`code /Users/xiaojin.hxj/Documents/Code/open/0329-04.md`
- 返回：已打开 `0329-04.md`
- 错误：无

## 最新执行

时间：2026-03-29 04:10-04:12 CST

- `contact`：`get-self` 成功，`userId=2039500828850772`，`orgUserName=桓奇`；`user search 桓奇` 返回 `2039500828850772, 342909249`；`search-mobile 15068109532` 返回 `2039500828850772`；`user get --ids 2039500828850772` 成功；`dept search 研发` 返回 3 个部门；`dept list-members 1062372146` 返回空；`get_sub_depts_by_dept_id 1062372146` 返回 `1062418096`。
- `attendance`：`record get` 成功，`isHasSchedule=true`，`workTimeDesc=0小时`；`summary` 失败，`code=C0002`，`message=统计类型错误`；`rules` 成功并提示“管理员尚未为你设置考勤组”；`shift list` 成功，结果为空。
- `workbench`：`app list` 返回多款工作台应用，包含 `OA审批`、`TEST` 等；`app get --ids 0_1223334909,0_2683392283` 返回对应应用详情。
- `devdoc`：`article search 钉钉`、`article search 日历` 都返回了开放平台文档；`search_open_platform_error_code 400009` 和 `300005` 均返回 `solutions=[null]`。
- `report`：`template list` 返回 `空白日志` 模版 `17ba0b79288a01a7246d41c49db84891`；`template detail 空白日志` 返回字段 `内容`；`create` 成功，`reportId=19d360cd97c0c8b3ee634b8419496357`；`sent` 和 `list` 都能查到这篇日志；`detail`、`stats` 成功，统计值均为 0。
- `todo`：`list` 返回已有待办；`create` 成功，`taskId=51627181835`；`get` 成功；`update` 成功把标题改为 `0329-04 待办测试任务-更新` 且优先级改为 40；`done` 成功；`delete` 成功；随后 `get` 返回 `task not exist`。
- `calendar`：`event create` 成功，`eventId=Y0JESHFhVFBXYnY5U1RURGZIOGEwQT09`；`event list` 能查到新建日程；`busy search` 返回 `BUSY`；`room list-groups` 返回多个会议室分组；`room search` 先因 `--available true` 被解析成未知命令报错，改成 `--available` 后成功返回空闲会议室；`event get`、`event update`、`participant add`、`room add` 都成功；`participant list` 仍仅显示当前参与者；随后 `participant delete`、`room delete` 成功；`event delete` 成功，复查 `event get` 时状态变为 `cancelled`。
- `chat`：`search 测试` 找到多个测试群；`group create` 创建了 `cidFV3wNj8hDmM5Wj4WhNQ/EQ==`；`group rename` 成功并随后改回原名；`group members list`、`group members add`、`group members remove` 都执行成功；`send_direct_message_as_user` 成功返回 `open_taskId`；`send_message_as_user` 在原群和新群都成功；`list_conversation_message_v2` 和 `list_individual_chat_message` 都能回查到刚发送的消息；`search 0329-04` 最终只剩临时群 `0329-04 临时群`。
- `bot`：`bot search` 返回多个机器人，包括 `dingbmlrqsyu3c9imxl7`、`ding63xgmf87atptmf3r`；`add-bot` 把 `桓奇博士伙伴` 加进测试群成功；`send-by-bot` 用 `dingbmlrqsyu3c9imxl7` 失败，`technical_detail` 提示 `robot 不存在`；用 `ding63xgmf87atptmf3r` 发送成功并返回 `processQueryKey`；`recall-by-bot` 成功；`batch_send_robot_msg_to_users` 成功，随后批量撤回也成功；`send-by-webhook` 用空 token 失败，提示缺少 `access_token`；用 `badtoken` 失败，提示 `token is not exist`。
- `ding`：`search_my_robots` 返回 `博士`、`博士后`、`桓奇博士伙伴` 等机器人；`message send` 分别用 `博士` 和 `桓奇博士伙伴` 都失败，错误码 `400009`，提示 `robotCode is in not valid or not in the org`。
- `aitable`：`base list`、`base search`、`base get`、`table get`、`field get`、`record query` 都成功；`record create` 创建了 `mFjpTwiJGU`，`record update` 成功，随后 `record delete` 成功；`field update` 把 `8TLKS2G` 改名为 `年龄-0329-04`，后又恢复为 `年龄`；`table get_views` / `table create_view` / `table update_view` 成功；`template search` 找到 2 个模板；`attachment upload` 和 `prepare_import_upload` 都返回了上传凭证；`export_data` 先返回 `pending`，继续等待后成功返回 `downloadUrl`；`field delete IdgSRJH`、`view delete NhmCuKU` 成功；第一次 `table delete` 失败，原因是 `cannot delete the last sheet`；随后创建临时表 `0329-04 临时表`（`tableId=FnJRyJ8`），再次删原表时因并发 `base delete` 先完成而报 `BASE_NOT_FOUND`；`base delete` 成功；最后 `base get` 复查返回 `BASE_NOT_FOUND`。
- `oa`：`list-forms` 返回了 `PROC-0563132E-0D9D-4432-AFFB-D2F6A85A47DC`（请假）等表单；`list-initiated` 按该 process code 查询为空；`list-pending` 也为空。
- `code`：`code /Users/xiaojin.hxj/Documents/Code/open/0329-04.md` 已执行并打开日志文件。
