# 成员管理

> 概念锚点：成员=谁能改这个应用（DEVELOPER 等角色）；见 SKILL.md 概念地图。

`dws dev app member list/add/remove` 管理应用成员。参数查 `dws schema dev.app.member.<method>`（add/remove 需 `--member-user-ids` 列表 + `--member-type`，如 DEVELOPER）。
