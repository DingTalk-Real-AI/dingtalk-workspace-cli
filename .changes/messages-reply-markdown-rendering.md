---
category: Fixed
---

- **Chat quote reply help** — `chat +messages-reply` no longer says a personal quote reply's content is shown as plain text; the body is sent as `text` and the DingTalk client renders its Markdown (headings, bold, inline code, lists).
