---
category: Fixed
---

- **Reliable upgrade and download flow** — resolves stable and beta checks from npm dist-tags with a 10-minute cache, bypasses the cache for explicit installs, preserves npm/pnpm ownership during updates, verifies bundled and direct-download assets with SHA256, and publishes retried downloads atomically.
