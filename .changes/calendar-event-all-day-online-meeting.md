---
category: Added
---

- **Calendar event options** — adds `--is-all-day` and `--add-online-meeting` to `calendar event create` and `update`, with date-only validation for explicit all-day events and omission-preserving boolean updates.
