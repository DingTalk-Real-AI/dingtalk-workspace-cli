---
category: Added
---

- **Contact user lookup by dingtalkId** — adds `dws contact user get-by-dingtalk-id --id <dingtalkId>` (alias `search-dingtalk`) to retrieve a user's `userId`, `uid`, and name from their dingtalkId.
